{% extends '::backend.html.twig' %}

{% block contenido %}
<!--indexBackend Proyecto-->


            <div class="navbar">
                <div class="navbar-inner">
                    <ul class="breadcrumb">
                        <i class="icon-chevron-left hide-sidebar"><a href='#' title="Hide Sidebar" rel='tooltip'>&nbsp;</a></i>
                        <i class="icon-chevron-right show-sidebar" style="display:none;"><a href='#' title="Show Sidebar" rel='tooltip'>&nbsp;</a></i>
                        <li>
                            <a href="{{path('backend')}}">Dashboard</a> <span class="divider">/</span>  
                        </li>
                        <li class="active">Proyectos</li>
                    </ul>
                </div>
            </div>
            <!-- block -->
            <div class="block">
                <div class="navbar navbar-inner block-header">
                    <div class="muted pull-left">Proyectos</div>
                </div>
                <div class="block-content collapse in">
                    <div class="span12">
                        <div class="table-toolbar">
                          <div class="btn-group">
                             <a href="{{path('proyecto_new')}}"><button class="btn btn-success">Add New <i class="icon-plus icon-white"></i></button></a>
                          </div>
                          <div class="btn-group">
                             <a href="{{path('proyecto_print')}}"><button class="btn btn-success">Download <i class="icon-download icon-white"></i></button></a>
                          </div>
                        </div>
                        <div class="mygrid-wrapper-div">
                          <table class="table" style="width:95%">
                            <thead>
                              <tr>
                                <th>Nombre</th>
                                <th>Fecha de Inicio</th>
                                <th>Fecha de Terminación</th>
                                <th>Estatus</th>
                                <th></th>
                                <th>Acction</th>
                              </tr>
                            </thead>
                            <tbody id="myTable" >
                                {% for entity in entities %}   
                                  <tr>
                                      <td>{{ entity.nombre }}</td>
                                      <td  nowrap>{{ entity.fechaInicio|date('d-m-Y') }}</td>
                                      <td  nowrap>{{ entity.fechaFinal|date('d-m-Y') }}</td>
                                      <td>{% if entity.status==1 %}
                                            Activo
                                          {% else %}
                                            Inactivo
                                          {% endif %}</td>
                                     <!-- <td>{{ entity.idEntidad }} </td>
                                      <td>{{ entity.montoFinanciero|number_format(2, '.', ',') }}</td>-->
                                      <td><a href="{{ path('proyecto_miembros', { 'id': entity.id }) }}" class="btn btn-primary btn-lg" >Miembros</a></td>
                                      <td>
                                              <ul>
                                                  <li>
                                                        <a href="{{ path('proyecto_delete', { 'id': entity.id }) }}">delete</a>
                                                  </li>
                                                  <li>
                                                        <a href="{{ path('proyecto_edit', { 'id': entity.id }) }}">edit</a>
                                                  </li>
                                              </ul>
                                      </td> 
                                  </tr>
              
                                {% endfor %}
          
                            </tbody>
                          </table>
                          
                        </div>  
                        <div class="col-md-12 text-center">
                            <ul class="pagination pagination-lg pager" id="myPager"></ul>
                          </div>
                    </div>
                </div>
            </div>
            <!-- /block -->

         

          <!-- Modal 
          <div class="modal fade bs-example-modal-lg" tabindex="-1" role="dialog" aria-labelledby="myLargeModalLabel" aria-hidden="true">
            <div class="modal-dialog modal-lg">
              <div class="modal-content">
                <div class="modal-header">
                  <h4 class="modal-title" id="myModalLabel">Miembros</h4>
                </div>
                <div class="modal-body">
                  ...
                </div>
                <div class="modal-footer">
                  <a href="{{ path('proyecto_backend') }}"  class="btn btn-default">Close</a>
                 
                </div>
                
              </div>
            </div>
          </div>-->

        </div>
{% endblock %}
