{% extends '::backend.html.twig' %}

{% block contenido -%}
		<div class="row-fluid">
            <div class="navbar">
                <div class="navbar-inner">
                    <ul class="breadcrumb">
                        <i class="icon-chevron-left hide-sidebar"><a href='#' title="Hide Sidebar" rel='tooltip'>&nbsp;</a></i>
                        <i class="icon-chevron-right show-sidebar" style="display:none;"><a href='#' title="Show Sidebar" rel='tooltip'>&nbsp;</a></i>
                        <li>
                            <a href="{{path('backend')}}">Dashboard</a> <span class="divider">/</span>  
                        </li>
                        <li>
                            <a href="{{path('recurso_backend')}}">Recursos</a> <span class="divider">/</span>  
                        </li>
                        <li class="active">Nuevo</li>
                    </ul>
                </div>
            </div>
            
			   
			   <fieldset>
                    <legend><h1>Recurso Nuevo</h1></legend>
                    {{ form_start(form, { 'action': path('recurso_new'), 'method': 'POST' }) }} 
                    
                        {{ form_label(form.nombre, 'Nombre') }}
                        {{ form_widget(form.nombre) }}
                        {{ form_label(form.descripcion, 'Descripción') }}
                        {{ form_widget(form.descripcion) }}
                        {{ form_label(form.file, 'Archivo') }}
                        {{ form_widget(form.file) }}
                        {{ form_label(form.idioma, 'Idioma') }}
                        {{ form_widget(form.idioma) }}
                     
                      
                    
                        
                        
                        <br>
                        <input type="submit" value="Save" >
                         <br>
                    {{ form_end(form) }}
                </fieldset>

			    
			
		</div>		
{% endblock %}
